// Cloudflare Worker URL — replace with your deployed URL
export const API_BASE = "https://ashtonk-lobbies.YOUR-SUBDOMAIN.workers.dev";

// Or use same-origin if hosted on the same Worker:
// export const API_BASE = "";
